{
  "name": "energy-wise-frontend",
  "version": "1.0.0",
  "description": "Frontend project with Jest and JSDOM testing",
  "scripts": {
    "test": "jest --watchAll",
    "coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^22.1.0"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "/__tests__/helpers.js"
    ]
  }
}
